environment: admin
clusterName: admin
clusterUrl: https://rancher.euwest.azure.internal.softwareheritage.org/k8s/clusters/c-q2wd4

metallb:
  enabled: true

ingressNginx:
  enabled: true
  defaultClass: true
  annotations:
    # To be able to share the same ip for several services
    # https://metallb.org/usage/#ip-address-sharing
    metallb.universe.tf/allow-shared-ip: internalIP
  requestedMemory: 100Mi
  config:
    worker-processes: 2

certManager:
  enabled: true
  webhookGandiEnabled: true
  useGandiDNS: true
