{{- if and .Values.indexerStorage.enabled .Values.indexerStorage.autoScaling -}}
{{- with .Values.indexerStorage.autoScaling -}}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  namespace: {{ $.Values.namespace }}
  name: indexer-storage
  labels:
    app: indexer-storage
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: indexer-storage
  minReplicas: {{ .minReplicaCount | default 2 }}
  maxReplicas: {{ .maxReplicaCount | default 10 }}
  metrics:
  {{- if .cpuPercentageUsage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .cpuPercentageUsage }}
  {{- end -}}
{{- end -}}
{{- end -}}
