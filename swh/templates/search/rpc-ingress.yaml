{{- if and .Values.search.enabled .Values.search.ingress.enabled }}
{{- include "swh.ingress" (dict "Values"        .Values
                                "serviceType"   "search-rpc"
                                "configuration" .Values.search) -}}
{{ end }}
