{{- if and .Values.search.enabled .Values.search.autoScaling -}}
{{- include "swh.autoscale" (dict "Values"        .Values
                                  "serviceType"   "search-rpc"
                                  "configuration" .Values.search) -}}
{{- end -}}
