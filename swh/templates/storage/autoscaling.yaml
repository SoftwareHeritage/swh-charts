{{- if and .Values.storage.enabled .Values.storage.autoScaling -}}
{{- with .Values.storage.autoScaling -}}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  namespace: {{ $.Values.namespace }}
  name: storage
  labels:
    app: storage
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: storage
  minReplicas: {{ .minReplicaCount | default 2 }}
  maxReplicas: {{ .maxReplicaCount | default 10 }}
  metrics:
  {{- if .cpuPercentageUsage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .cpuPercentageUsage }}
  {{- end -}}
{{- end -}}
{{- end -}}
