{{ if and .Values.scrubber.enabled .Values.scrubber.storageChecker.enabled -}}
{{- include "swh.secrets.multiInstanceService.vaultStaticSecrets"
      (dict "topLevelConfig" .Values.scrubber.storageChecker
            "Values" .Values) -}}
{{ end }}
