{{ if .Values.vault.enabled -}}
---
apiVersion: v1
kind: Service
metadata:
  name: vault-rpc
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: vault-rpc
  ports:
    - port: 5005
      targetPort: 5005
      name: rpc
{{ end }}
