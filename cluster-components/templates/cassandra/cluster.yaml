{{ if .Values.cassandra.enabled -}}
{{- $storageConfig := .Values.cassandra.storageConfig -}}
{{- range $clusterName, $clusterConfig := .Values.cassandra.deployments -}}
---
apiVersion: k8ssandra.io/v1alpha1
kind: K8ssandraCluster
metadata:
  name: {{ $clusterName }}
  namespace: {{ $.Values.cassandra.namespace }}
spec:
  cassandra:
    serverVersion: "4.0.1"
    config:
      cassandraYaml:
        enable_scripted_user_defined_functions: true
        enable_user_defined_functions: true
      jvmOptions:
        heapSize: 512M
    {{- if $storageConfig }}
    storageConfig:
    {{- toYaml $storageConfig | nindent 6 }}
    {{ end }}

    datacenters:
    {{- range $dataCenterConfig := $clusterConfig.datacenters }}
      - metadata:
          name: {{ $dataCenterConfig.name }}
        size: {{ $dataCenterConfig.replicas }}
        # stargate:
        #   size: 1
        #   heapSize: 256M
    {{ end }}
{{ end }}
{{ end }}
