{{- $namespace := .Values.kafkabackup.namespace -}}
{{ if and
  (.Values.kafkabackup.enabled)
  (ne $namespace "cluster-components")
}}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ $namespace }}

{{ end }}
