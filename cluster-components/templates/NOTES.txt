This installed/upgraded:
{{- if .Values.alertmanagerIrcRelay.enabled }}
- An alert manager irc relay. Any alerts sent from alertmanager are propagated to irc's
  #{{ .Values.alertmanagerIrcRelay.room }} room through the irc relay
  http://alertmanager-irc-relay:{{ .Values.alertmanagerIrcRelay.http_port }}/{{ .Values.alertmanagerIrcRelay.room }}
{{ end }}
{{- if and .Values.prometheus.enabled .Values.prometheus.alertmanager.enabled .Values.prometheus.alertmanager.ircrelay.enabled }}
- Allows to relay alertmanager's alerts to the relay exposed at {{ .Values.prometheus.alertmanager.ircrelay.url }}
{{ end }}
{{- if .Values.blackboxExporter.enabled }}
- blackbox exporter. This installs probes to expand monitoring to http(s), DNS, ...
  endpoints (e.g. https://www.softwareheritage.org, ...)
{{ end }}
