{{- if .Values.openbao.rbac.enabled -}}
{{- if not .Values.openbao.enabled }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: openbao
{{- end }}
# To authorize the openbao cronjob orchestration to write secrets in the client cluster
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: openbao-secret-writer
  namespace: vso
rules:
- apiGroups: [""]
  resources: ["secrets"]
  # Authorized to create and delete secret (for renewals)
  verbs: ["create", "delete"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: openbao-secret-writer-binding
  namespace: vso
subjects:
- kind: ServiceAccount
  name: default
  namespace: openbao
roleRef:
  kind: Role
  name: openbao-secret-writer
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: openbao-vso-auth
spec:
  method: appRole
  mount: secrets-{{ .Values.clusterName }}
  appRole:
    roleIdSecretRef:
      name: openbao-vso-auth-role-id-secret
      key: id
    secretIdSecretRef:
      name: openbao-vso-auth-role-secret-id-secret
      key: id
  allowedNamespaces:
    - "*"

{{- end }}
